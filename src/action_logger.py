"""User action logger for Taiwan Weather Map (HW1 extension).

Logs meaningful user interactions (e.g. view location, layer changes) into SQLite.
Implementation will be added in subsequent phases.
"""
